import React from 'react';
import PagePlaceholder from '../components/PagePlaceholder';

export default function Geofences() {
  return (
    <PagePlaceholder
      eyebrow="Coming next"
      title="Geofencing"
      description="Draw and save service-area boundaries on the map, and see which vehicles have breached them. Needs the geofence table and boundary-check logic from the backend first."
    />
  );
}
